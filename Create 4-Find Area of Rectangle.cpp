#include <iostream>
using namespace std;

int main() {
    //Write your code here
    int L,B,area;
    cin >> L;
    cin >> B;
    area = L*B;
    cout << area;
    return 0;
}

// Declare three integer variables: L, B, and area.
// user to input the length of the rectangle using cin >> L;. The user to enter the length on the console, and the program will store it in the variable L.
// the user to input the width of the rectangle using cin >> B;. The user to enter the width on the console, and the program will store it in the variable B.
// Calculate the area of the rectangle by multiplying the length (L) and the width (B) and store the result in the variable area.
// Output the calculated area using cout << area;. The program will display the calculated area of the rectangle on the console.
// Terminate the program by returning 0 from the main function.
